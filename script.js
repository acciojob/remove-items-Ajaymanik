const select = document.getElementById('colorSelect');
const buttonL =document.querySelector('input[type="button"]');
buttonL.addEventListener('click' , () => {
	select.remove(select.selectdIndex);
})
